<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flashcard"
        tools:targetApi="31">

        <!-- MainActivity là màn hình khởi chạy chính -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ THÊM KHAI BÁO CHO STUDYACTIVITY Ở ĐÂY -->
        <activity
            android:name=".StudyActivity"
            android:exported="false" />

        <!-- ✅ THÊM LUÔN KHAI BÁO CHO MATCHACTIVITY ĐỂ TRÁNH LỖI TƯƠNG TỰ -->
        <activity
            android:name=".MatchActivity"
            android:exported="false" />

        <!-- VocabularyListActivity để hiển thị danh sách từ vựng -->
        <activity
            android:name=".VocabularyListActivity"
            android:exported="false" />

        <!-- AIChatActivity để chat tự do với AI -->
        <activity
            android:name=".AIChatActivity"
            android:exported="false" />

    </application>

</manifest>
